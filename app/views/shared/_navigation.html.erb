<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Desafio</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if current_member %>
                <li class="nav-item">
                        <%= link_to "#{t('Tasks')}", tasks_path , class: "dropdown-item"%>
                </li>
                <% end %>
                
            </ul>
            <% if current_member %> 
                <ul class="navbar-nav">
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false"> 
                            
                            <%= current_member.email %> 
                        
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            
                            <li>
                                <%= link_to t('edit'),edit_member_registration_path(current_member), class: "dropdown-item" %>
                            </li>
                            <li><hr class="dropdown-divider" /></li>
                            <li>
                                <%= link_to t('logout'), destroy_member_session_path, data: { "turbo-method": :delete }, class: "dropdown-item" %>
                            </li>
                    </li>
                </ul>
                <% else %>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <%= link_to 'Login', new_member_session_path, class: "dropdown-item" %>
                    </li>
                </ul>
            <% end %>
        </div>
    </div>
</nav>